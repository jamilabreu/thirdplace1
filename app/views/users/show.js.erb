var $user = $('#<%= @user.id %>')
var $profile = $user.find(".user-main")
$user.toggleClass('is-expanded');
$user.find('.user-name i').toggleClass('fa-angle-right fa-times');
if ($profile.html().length === 0) {
  $profile.html("<%= j render partial: 'main', locals: { user: @user } %>");
}